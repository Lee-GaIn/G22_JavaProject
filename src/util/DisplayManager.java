package util;

/**
 * The DisplayManager was created to manage to display the menu to the user.
 */

public class DisplayManager {
    // Methods
    public static void displayMenu(String menu) {
        // This method accepts string "menu"
        // and displays menu.

        System.out.print(menu);
    }
}
